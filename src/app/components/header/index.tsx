import { Box } from "@mui/material";
import React from "react";
import { HeaderStyle } from "./header.style";

const Header = () => {
  return (
    <HeaderStyle>
      <Box className="header___text">Header</Box>
    </HeaderStyle>
  );
};

export default Header;
